services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ziknet_backend
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
    command: node src/server.js

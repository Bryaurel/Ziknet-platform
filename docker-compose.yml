version: '3'
services:
  ziknet-backend:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=3000
      - JWT_SECRET=secret
